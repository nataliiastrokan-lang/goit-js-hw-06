# 📘 JavaScript Homework 06

## 📌 Topic: `this`, Objects & Classes

This project contains solutions for working with object methods, the `this` keyword, and ES6 classes with private fields.

---

## 🚀 Live Demo
👉 https://nataliiastrokan-lang.github.io/goit-js-hw-06/


---

## 🧩 Tasks Overview

### ✅ Task 1 — Customer Account
Refactored object methods using `this` to correctly access and modify object properties.

---

### ✅ Task 2 — Storage Class
Implemented a `Storage` class:
- Uses **private field** `#items`
- Provides methods:
  - `getItems()`
  - `addItem()`
  - `removeItem()`

---

### ✅ Task 3 — StringBuilder Class
Implemented a `StringBuilder` class:
- Uses **private field** `#value`
- Supports string manipulation:
  - `padStart()`
  - `padEnd()`
  - `padBoth()`

---

## 🛠️ Technologies Used

- JavaScript (ES6+)
- OOP concepts:
  - `this`
  - `class`
  - `constructor`
  - private fields (`#`)
- Array methods:
  - `filter()`

---

## 💡 Key Concepts

- Object-oriented programming in JavaScript
- Encapsulation using private class fields
- Working with `this` in object methods
- Class-based architecture
- Data manipulation without exposing internal state


